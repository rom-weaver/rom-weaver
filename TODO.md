# TODO

- Enlarge the annotation superscript numbers in docs table cells; they are too small to read at the current size.
- Give the docs "ON THIS PAGE" rail a background on desktop so it reads as a panel.
- Reduce the indent on the docs annotation note blocks (the `blockquote` under each table).
