# `@rom-weaver/darwin-arm64`

Native macOS arm64 binary for [`rom-weaver`](https://www.npmjs.com/package/@rom-weaver/cli).

This package is installed automatically by `@rom-weaver/cli` on Apple Silicon Macs. Most users should install the CLI package instead.
