# `@rom-weaver/darwin-x64`

Native macOS x64 binary for [`rom-weaver`](https://www.npmjs.com/package/@rom-weaver/cli).

This package is installed automatically by `@rom-weaver/cli` on Intel Macs. Most users should install the CLI package instead.
