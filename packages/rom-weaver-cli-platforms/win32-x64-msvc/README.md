# `@rom-weaver/win32-x64-msvc`

Native Windows x64 MSVC binary for [`rom-weaver`](https://www.npmjs.com/package/@rom-weaver/cli).

This package is installed automatically by `@rom-weaver/cli` on compatible Windows systems. Most users should install the CLI package instead.
