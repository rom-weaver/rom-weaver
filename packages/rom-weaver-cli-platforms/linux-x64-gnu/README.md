# `@rom-weaver/linux-x64-gnu`

Native Linux x64 glibc binary for [`rom-weaver`](https://www.npmjs.com/package/@rom-weaver/cli).

This package is installed automatically by `@rom-weaver/cli` on compatible Linux systems. Most users should install the CLI package instead.
