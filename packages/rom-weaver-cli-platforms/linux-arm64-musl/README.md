# `@rom-weaver/linux-arm64-musl`

Native Linux arm64 musl binary for [`rom-weaver`](https://www.npmjs.com/package/@rom-weaver/cli).

This package is installed automatically by `@rom-weaver/cli` on arm64 Linux, including glibc systems. Most users should install the CLI package instead.
