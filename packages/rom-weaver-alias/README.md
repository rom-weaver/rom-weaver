# `rom-weaver`

The unscoped convenience alias for [`@rom-weaver/cli`](https://www.npmjs.com/package/@rom-weaver/cli).

Install it with:

```sh
npm install --global rom-weaver
```

For the canonical package, use [`@rom-weaver/cli`](https://www.npmjs.com/package/@rom-weaver/cli).
