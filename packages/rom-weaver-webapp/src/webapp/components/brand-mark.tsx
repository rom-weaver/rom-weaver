import { BRAND_MARK_SRC } from "virtual:rom-weaver-brand-marks";

// The adjacent brand name supplies the accessible label.
const BrandMark = () => <img alt="" className="brand-mark" height={44} src={BRAND_MARK_SRC} width={44} />;

export { BrandMark };
