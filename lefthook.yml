pre-commit:
  parallel: false
  commands:
    rust-format:
      run: cargo fmt --check
    rust-clippy:
      run: cargo clippy --workspace --all-targets -- -D warnings -A clippy::style -A clippy::complexity -A clippy::perf
    wasm-threaded-check:
      run: 'RUSTFLAGS="${RUSTFLAGS:-} -D warnings" scripts/wasm/with-wasi-env.sh cargo check -p rom-weaver-containers --target wasm32-wasip1-threads --lib'
    whitespace:
      run: git diff --cached --check
    thread-guard-regressions:
      run: scripts/check-thread-guards.sh
