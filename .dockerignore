.git
.codegraph
.worktrees
target
**/node_modules
**/dist
**/.vitest-attachments
**/__screenshots__
prototype
third_party
*.log

# Not build inputs. These change often (docs and CI commits), and anything left
# here invalidates the `COPY . .` layer and with it the whole wasm+cargo build.
# Root *.md stays: several are read at build time (README as the workspace
# `readme`, CHANGELOG by the webapp version script's fallbacks).
.github
docs
